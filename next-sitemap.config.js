/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://girosbol.com', // Reemplaza con tu dominio
    generateRobotsTxt: true, // Genera también un robots.txt
    changefreq: 'daily', // Frecuencia de cambio
    priority: 0.7, // Prioridad de las páginas
    sitemapSize: 5000, // Máximo de URLs por sitemap
}
